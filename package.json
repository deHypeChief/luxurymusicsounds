{
  "name": "luxurymusicsounds",
  "private": true,
  "description": "Luxury Music Sounds: a static site, with tickets sold on Paystack",
  "scripts": {
    "dev": "bun --cwd=client run dev",
    "build": "bun --cwd=client run build",
    "preview": "bun --cwd=client run preview",
    "lint": "bun --cwd=client run lint",
    "check": "bun run lint && bun run build",
    "setup": "bun --cwd=client install"
  }
}
